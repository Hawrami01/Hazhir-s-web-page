// Simple JavaScript for extra functionality
document.addEventListener('DOMContentLoaded', () => {
    // Add event listeners or functions if needed, for example:
    console.log("Resume website loaded");
});
